# Local development works without credentials. Production requires Supabase.
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Generate a long random secret for the scheduled server endpoint.
JOB_SECRET=
# Demo mode allows visibly simulated GPS and shortened timers.
NEXT_PUBLIC_DEMO_MODE=true
